{
  "name": "bottelegram",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --exec node server.js",
    "dev": "nodemon --exec node server.js",
    "build": "babel server.js -d build --copy-files",
    "build-src": "babel server.js -d build --copy-files"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.25.9",
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "dotenv": "^16.4.5",
    "nodemon": "^3.1.7"
  }
}
